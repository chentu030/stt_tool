# Copy values into frontend/.env.local or Vercel env (do not commit real tunnel URLs).
# After deploy, read the URL from: gcloud compute ssh … → tmux capture-pane -t tunnel
#
# Example (ephemeral — changes when cloudflared restarts):
# STEEL_BASE_URL=https://xxxx.trycloudflare.com

STEEL_BASE_URL=
